# Agent Notes

- Always activate the project virtual environment before running Python tooling:
  - `source /Users/jphavill/Documents/github/garmin-weight-input/.venv/bin/activate`

## Test validation

- Use the test suite to validate changes after implementation.
- Run tests from the project root with the virtual environment activated:
  - `source /Users/jphavill/Documents/github/garmin-weight-input/.venv/bin/activate`
  - `pytest -q`
